# Threadloom 项目审计报告

**日期：** 2026-05-23  
**版本：** 1.2  
**范围：** 全仓库（结构、安全、代码质量、依赖、配置）

---

## 项目概况

| 维度 | 数据 |
|------|------|
| 后端代码 | ~29,000 行 Python（55 个 .py 文件） |
| 前端代码 | ~7,000 行（原生 JS + HTML + CSS，无框架） |
| 测试代码 | ~10,000 行（30 个 test 文件，pytest） |
| 依赖 | 5 个（bcrypt, Pillow, requests, pytest, basedpyright） |
| 运行时 | Python 3.11，stdlib `http.server.ThreadingHTTPServer` |
| 数据存储 | 纯文件系统（JSON + JSONL + Markdown），无数据库 |

---

## 安全评估

**总体评价：安全水平良好，核心安全机制完备。**

### ✅ 已做好的安全措施

1. **认证体系**：bcrypt 密码哈希（cost 12）、SHA-256 token 存储、30 天 TTL、每用户最多 10 个活跃 token
2. **登录防护**：per-IP 12次/分钟 + 全局 80次/分钟 限速、5 次失败后锁定 15 分钟、timing-safe 比较（dummy hash 防用户名枚举）
3. **CSRF 防护**：POST/DELETE/PUT 强制 Bearer header，拒绝 Cookie 认证
4. **SSRF 防护**：`safe_http.py` 实现 DNS 预解析 + IP 固定，拒绝私有/保留地址
5. **路径遍历防护**：`confine_to_root()` + `confine_to_user_root()` 严格限制文件访问范围
6. **CSP**：`script-src 'self'; object-src 'none'; frame-ancestors 'none'`
7. **绑定地址检查**：非 loopback 启动时强制要求多用户 + 管理员密码
8. **文件权限**：`users.json` / `sessions.json` 自动收紧到 0600
9. **Token 安全**：`is_safe_session_token()` 验证字符集后才写入 Set-Cookie
10. **危险调用控制**：核心服务路径未见 `eval`/`exec`/`subprocess`/`os.system` 等危险调用，无 SQL

### ⚠️ 当前风险点

| 级别 | 问题 | 说明 |
|------|------|------|
| 低 | CSP `style-src 'unsafe-inline'` | 降低了样式注入防护，但 `script-src 'self'` 仍有效 |
| 低 | 反向代理后 IP 限速失效 | `client_address[0]` 在代理后全变成 127.0.0.1；需配合代理层限速 |
| 低 | 部分文件写入非原子 | 2026-05-10 审计后已修复核心路径，但 `card_importer` 等仍有少量直接写入 |
| 信息 | `tmp/` 目录含较多测试数据 | 不影响安全，但占磁盘空间 |

---

## 代码质量

### 优点

- 测试覆盖面广（30 个测试文件覆盖核心链路）
- 模块职责清晰（narrator / state_keeper / selector / arbiter 分层）
- 原子 IO 已统一到 `atomic_io.py`
- 类型检查已配置（basedpyright）

### 问题

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 单文件过大 | 中 | `state_bridge.py`(122K)、`state_updater.py`(96K)、`state_keeper.py`(95K)、`handler_message.py`(70K) 等超大文件，维护困难 |
| 异常吞没 | 中 | 大量 bare `except Exception: pass`，bug 被静默掩盖 |
| 并发安全 | 中 | `ThreadingHTTPServer` 下部分模块缺乏锁保护（已有 session 级锁和 user/config 锁，但非全覆盖） |
| 本地缓存残留 | 低 | 工作区存在 `__pycache__` 编译缓存；若未被 git 跟踪，不影响仓库质量 |

---

## 依赖评估

```
bcrypt>=4.1,<5       ✅ 密码哈希，活跃维护
Pillow>=10,<12       ✅ 图片处理（头像/封面），活跃维护
requests>=2.31,<3    ⚠️ 核心服务路径待确认是否仍使用 requests
pytest>=8,<9         ✅ 测试框架
basedpyright>=1.31,<2 ✅ 类型检查
```

**建议**：确认 `requests` 是否仍存在运行时依赖；若核心路径与维护脚本均不再使用，可考虑移除。版本范围约束合理（上界锁定大版本）。

---

## 架构观察

1. **无框架依赖**：后端用 stdlib `http.server`，前端原生 JS。优点是零供应链风险，缺点是手动实现路由/中间件/错误处理。
2. **文件系统即数据库**：所有状态存 JSON/JSONL/Markdown。适合单机自托管场景，但并发写入需要锁保护。
3. **多用户隔离**：每用户独立目录树 (`runtime-data/{user_id}/`)，路径遍历检查到位。
4. **LLM 调用链**：通过 `safe_http` 的 IP-pinned 连接调用外部 API，SSRF 防护完善。

---

## 建议优先级

1. **高**：清理异常吞没（至少在持久化/认证路径上加 logging）
2. **中**：拆分超大文件（`state_bridge.py` 等），提升可维护性
3. **中**：确认 `requests` 依赖是否需要；若不需要则移除
4. **低**：部署时配合反向代理做 IP 限速（Nginx `limit_req`）
5. **低**：清理 `__pycache__` 残留和 `tmp/` 大文件

---

## 与历史审计对比

| 历史问题 | 状态 |
|----------|------|
| M1 — token 反射到 Set-Cookie 无验证 | ✅ 已修复（`is_safe_session_token()` 字符集校验） |
| L5 — `local_model_client` 绕过 SSRF 防护 | ✅ 已修复（改用 `open_safe_connection`） |
| localStorage token 泄露 | ✅ 已修复（仅内存持有） |
| 非原子写入 | ✅ 核心路径已修复 |
| pyright 类型错误 | ✅ 已修复 |
| 依赖管理缺失 | ✅ 已新增 `requirements.txt` |

---

**结论**：项目安全基础扎实，此前审计与代码审查中发现的问题已基本修复。当前无部署阻断级漏洞。主要改进空间在代码可维护性（文件过大、异常处理）而非安全性。
